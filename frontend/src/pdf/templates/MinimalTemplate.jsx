/**
 * MinimalTemplate.jsx
 *
 * Placeholder — coming in a future release.
 */

import ProfessionalTemplate from "./ProfessionalTemplate";

const MinimalTemplate = (props) => <ProfessionalTemplate {...props} />;

export default MinimalTemplate;
