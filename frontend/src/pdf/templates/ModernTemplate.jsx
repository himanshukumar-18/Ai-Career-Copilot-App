/**
 * ModernTemplate.jsx
 *
 * Placeholder — coming in a future release.
 * The router in ResumePDF.jsx will fall back to ProfessionalTemplate
 * if this template is selected before it is complete.
 */

import ProfessionalTemplate from "./ProfessionalTemplate";

const ModernTemplate = (props) => <ProfessionalTemplate {...props} />;

export default ModernTemplate;
